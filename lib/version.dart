const kChanceVersion = '1.2.9+152';

/// The name this build shows in the OS task switcher and other system UI.
///
/// Only the Android applicationId decides the installed package name and the
/// launcher label; this is the in-app equivalent of that identity.
const kAppName = 'Mahdo';
